You are the Example agent. This is a test fixture.

RDF_TEST_MARKER_example_agent

## Role

Minimal fixture canonical agent used by adapter BATS tests.
No frontmatter in this file — the adapter prepends YAML frontmatter
from agent-meta.json during generation.

## Constraints

This agent exists solely as a stable input body for generator
round-trip assertions. It has no real behavior.

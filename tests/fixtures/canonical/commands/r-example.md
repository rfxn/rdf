You are running the /r-example command. This is a test fixture.

RDF_TEST_MARKER_r_example

## Purpose

Minimal fixture canonical command used by adapter BATS tests.
No frontmatter — the adapter outputs commands as direct copies.

## Usage

This command does nothing at runtime; it exists solely as a
stable input body for generator round-trip assertions.

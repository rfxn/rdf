# RDF 2.0 — Phase 5 Implementation Plan: Agent Teams Architecture

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development
> (if subagents available) or superpowers:executing-plans to implement this plan.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a dispatch abstraction layer that allows the EM pipeline to
operate identically in two modes: subagent mode (current — Agent tool dispatch)
and Agent Teams mode (future — TeammateTool with shared task list and
inter-agent messaging). Feature-flagged, backward-compatible, zero regression.

**Architecture:** A `lib/dispatch.sh` library provides `rdf_dispatch()` as the
single entry point for all agent invocations. When `RDF_AGENT_TEAMS=false`
(default), it generates subagent dispatch instructions (current behavior). When
`RDF_AGENT_TEAMS=true`, it generates Agent Teams dispatch instructions using
TeamCreate, TaskCreate, and teammate spawning. The adapter layer
(`adapters/claude-code/`) gains Agent Teams configuration metadata. The mgr
agent and command are updated to consume the dispatch abstraction.

**Spec:** `docs/specs/2026-03-16-rdf-2.0-architecture-design.md` §8 Phase 5

**Tech Stack:** Bash 4.1+, JSON (jq), Claude Code Agent Teams API (experimental)

**Dependencies:** Phase 2 (adapter pattern, CLI, `rdf_common.sh`). Independent
of Phase 3 (profiles) and Phase 4 (state refresh).

---

## Agent Teams API Research Summary

Research conducted 2026-03-16 from official documentation at
https://code.claude.com/docs/en/agent-teams and community references.

### API Surface

Agent Teams is experimental (requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
in `settings.json` or environment). Available since Claude Code v2.1.32.

**Seven tool primitives:**

| Tool | Purpose | Key Parameters |
|------|---------|----------------|
| `TeamCreate` | Initialize team namespace | `team_name`, `description` |
| `TaskCreate` | Define work items | `subject`, `description`, `activeForm` |
| `TaskUpdate` | Change task state/ownership | `taskId`, `status`, `owner`, `addBlockedBy` |
| `TaskList` | List all tasks with status | (none) |
| `Task` (with `team_name`) | Spawn teammate | `team_name`, `name`, `subagent_type`, `prompt`, `model` |
| `SendMessage` | Inter-agent communication | `type`, `recipient`, `content`, `summary` |
| `TeamDelete` | Remove team resources | (none) |

**Task states:** `pending` -> `in_progress` -> `completed`
**Task dependencies:** `addBlockedBy` blocks until predecessors complete.
**File locking:** Prevents race conditions on concurrent task claims.

**Message types:** `message`, `broadcast`, `shutdown_request`,
`shutdown_response`, `plan_approval_request`, `plan_approval_response`

**File structure:**
```
~/.claude/teams/{team-name}/config.json
~/.claude/teams/{team-name}/inboxes/{agent-name}.json
~/.claude/tasks/{team-name}/1.json
```

**Teammate environment variables (auto-set):**
```
CLAUDE_CODE_TEAM_NAME="team-name"
CLAUDE_CODE_AGENT_ID="worker-1@team-name"
CLAUDE_CODE_AGENT_NAME="worker-1"
```

**Delegate mode:** `Shift+Tab` restricts lead to coordination-only (no code
writes). Maps directly to EM's existing constraint: "You manage projects — you
do NOT write code."

**Built-in agent types:** `general-purpose` (all tools), `Explore` (read-only),
`Plan` (read-only), `Bash` (shell only).

**Hooks:**
- `TeammateIdle`: fires when teammate goes idle. Exit code 2 = feedback + keep working.
- `TaskCompleted`: fires when task marked complete. Exit code 2 = reject completion.

### Subagent vs Agent Teams: Decision Matrix

| Criterion | Subagent | Agent Teams |
|-----------|----------|-------------|
| Context | Own window, results return to caller | Fully independent |
| Communication | Report back to main agent only | Teammates message each other directly |
| Coordination | Main agent manages all work | Shared task list with self-coordination |
| Token cost | Lower (results summarized back) | Higher (each is a full session) |
| Best for | Focused tasks (SE, QA, Scope) | Parallel exploration, competing hypotheses |

### Mapping to rfxn Pipeline

| Pipeline Stage | Subagent Mode (current) | Agent Teams Mode (future) |
|----------------|------------------------|--------------------------|
| EM startup | Single session, reads state | Team lead session, delegate mode |
| Scope dispatch | Agent tool -> rfxn-scope | TaskCreate + teammate spawn |
| SE dispatch | Agent tool -> rfxn-sys-eng | TaskCreate + teammate spawn |
| Challenger | Agent tool -> rfxn-sys-challenger | TaskCreate (blocked by SE plan task) |
| QA + Sentinel | Parallel Agent tool calls | Parallel teammates, shared task list |
| UAT | Agent tool -> rfxn-sys-uat | TaskCreate (blocked by QA task) |
| Result collection | Read work-output/ files | Read work-output/ + inbox messages |
| Pipeline look-ahead | Worktree isolation | Teammates + git worktree |

### Key Insight: Dispatch Is Instructions, Not Execution

The dispatch abstraction does not execute agent spawning directly — it
**generates structured instructions** that the mgr command/agent follows. In
subagent mode, these are Agent tool call specifications. In Agent Teams mode,
these are TeamCreate/TaskCreate/Task tool call specifications with dependency
chains. The EM agent (mgr.md) interprets these instructions and makes the
actual tool calls.

This is important because:
1. The dispatch library is pure bash — it runs in `rdf generate` context
2. The actual tool calls happen in the LLM's context window
3. The abstraction produces markdown/JSON instructions, not API calls

---

## File Structure

### Files to Create

| File | Responsibility | Lines |
|------|---------------|-------|
| `lib/dispatch.sh` | Dispatch abstraction — mode-aware agent invocation | ~250 |
| `lib/dispatch-agents.json` | Agent registry — roles, models, tools, constraints | ~120 |
| `lib/dispatch-tasks.json` | Task templates — pipeline stages as reusable task definitions | ~150 |
| `adapters/claude-code/teams-meta.json` | CC Agent Teams configuration metadata | ~60 |
| `docs/specs/agent-teams-research.md` | API research document (from above) | ~200 |
| `docs/specs/dispatch-abstraction.md` | Dispatch interface specification | ~150 |

### Files to Modify

| File | Change |
|------|--------|
| `canonical/agents/mgr.md` | Add dispatch abstraction consumption, dual-mode awareness |
| `canonical/commands/mgr.md` | Update dispatch sections to reference `rdf dispatch` output |
| `adapters/claude-code/adapter.sh` | Add Agent Teams config generation (when Phase 2 creates it) |
| `lib/rdf_common.sh` | Add `rdf_feature_flag()` helper |
| `bin/rdf` | Add `dispatch` subcommand |
| `CHANGELOG` | Phase 5 entries |
| `CHANGELOG.RELEASE` | Phase 5 entries |

### Directories to Create

- `docs/specs/` (may already exist from Phase 2)

---

## Chunk 1: Research Documentation + Dispatch Specification

### Task 5.1: Write Agent Teams Research Document

**Files:**
- Create: `docs/specs/agent-teams-research.md`

- [ ] **Step 5.1.1: Create docs/specs/ directory if needed**

```bash
mkdir -p docs/specs
```

- [ ] **Step 5.1.2: Write agent-teams-research.md**

This document captures the API research for future reference. Content is the
research summary from above, expanded with full tool signatures, message
formats, and configuration examples.

```markdown
# Claude Code Agent Teams — API Research

> Researched 2026-03-16. Source: https://code.claude.com/docs/en/agent-teams
> Status: Experimental (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1)
> Minimum version: Claude Code v2.1.32

## 1. Architecture

Agent Teams coordinate multiple Claude Code instances. One session is the
**team lead** (coordinator); others are **teammates** (independent workers).

Components:
- **Team lead**: main CC session, creates team, spawns teammates, coordinates
- **Teammates**: separate CC instances, own context windows, self-claim work
- **Task list**: shared work items with dependency tracking and file locking
- **Mailbox**: inbox-based messaging between any two agents

Storage:
- Team config: `~/.claude/teams/{team-name}/config.json`
- Task list: `~/.claude/tasks/{team-name}/{id}.json`
- Inboxes: `~/.claude/teams/{team-name}/inboxes/{agent-name}.json`

## 2. Tool Primitives

### TeamCreate
```json
{ "team_name": "rfxn-pipeline", "description": "rfxn engineering pipeline" }
```
Creates `~/.claude/teams/{team_name}/config.json` with members array.

### TaskCreate
```json
{
  "subject": "SE: Implement Phase N",
  "description": "Execute 7-step SE protocol for phase N...",
  "activeForm": "Implementing phase N"
}
```
Creates `~/.claude/tasks/{team_name}/{id}.json`. Fields: id, subject,
description, status (pending), owner (empty).

### TaskUpdate
```json
{ "taskId": "1", "status": "in_progress", "owner": "sys-eng" }
{ "taskId": "3", "addBlockedBy": ["1", "2"] }
```
States: pending, in_progress, completed. File locking prevents double-claims.
When a blocking task completes, blocked tasks auto-unblock.

### TaskList
No parameters. Returns all tasks with current status, subject, owner.

### Task (teammate spawn — extended with team_name)
```json
{
  "team_name": "rfxn-pipeline",
  "name": "sys-eng",
  "subagent_type": "general-purpose",
  "prompt": "You are the Senior Engineer...",
  "model": "opus",
  "run_in_background": true
}
```
Spawns a teammate that can access TaskList, SendMessage, and project context.

### SendMessage
```json
{ "type": "message", "recipient": "team-lead", "content": "Task complete.", "summary": "Done" }
{ "type": "broadcast", "name": "team-lead", "value": "Status update" }
{ "type": "shutdown_request", "target_agent_id": "sys-eng", "reason": "Pipeline complete" }
{ "type": "shutdown_response", "request_id": "shutdown-123", "approve": true }
{ "type": "plan_approval_response", "request_id": "plan-xyz", "approved": true }
```

### TeamDelete
No parameters. Removes team config and task directories. Fails if active
teammates exist (must shutdown first).

## 3. Delegate Mode

Activated via Shift+Tab in interactive mode. Restricts lead to:
- Task management (create, update, list)
- Communication (message, broadcast)
- Teammate lifecycle (spawn, shutdown)
- NO code writes, NO test execution, NO file modifications

Maps directly to EM's constraint: "You manage projects, you do NOT write code."

## 4. Hooks

### TeammateIdle
Fires when teammate is about to go idle. Exit code 2 sends feedback and keeps
teammate working. Use for auto-assigning follow-up work.

### TaskCompleted
Fires when task is being marked complete. Exit code 2 prevents completion and
sends feedback. Use for quality gates (lint, test pass requirements).

## 5. Teammate Environment Variables

Set automatically when spawned as teammate:
- CLAUDE_CODE_TEAM_NAME — team namespace
- CLAUDE_CODE_AGENT_ID — "name@team-name"
- CLAUDE_CODE_AGENT_NAME — agent name

## 6. Configuration

Enable in settings.json:
```json
{ "env": { "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1" } }
```

Display modes: "auto" (default), "in-process", "tmux"
```json
{ "teammateMode": "in-process" }
```

## 7. Limitations (as of 2026-03-16)

- No session resumption for in-process teammates
- Task status can lag (teammates may forget to mark complete)
- One team per session, no nested teams
- Lead is fixed for session lifetime
- All teammates inherit lead's permission settings at spawn
- Split panes require tmux or iTerm2
- Shutdown can be slow (waits for current tool call)

## 8. Token Cost Model

Each teammate has its own context window. Cost scales linearly with team size.
3-teammate team uses approximately 3-4x single-session tokens.

Optimization: Use Sonnet for focused implementation teammates, Opus for lead
coordination and semantic-depth roles (sys-eng, sys-sentinel).
```

- [ ] **Step 5.1.3: Commit research document**

```bash
git add docs/specs/agent-teams-research.md
git commit -m "$(cat <<'EOF'
Add Agent Teams API research document

[New] docs/specs/agent-teams-research.md — comprehensive API surface
  documentation from official CC docs research (2026-03-16)
[New] Tool primitives, message formats, delegate mode, hooks, limitations
[New] Token cost model and optimization guidance
EOF
)"
```

---

### Task 5.2: Write Dispatch Abstraction Specification

**Files:**
- Create: `docs/specs/dispatch-abstraction.md`

This is the design document for the dispatch abstraction layer — the interface
contract that `lib/dispatch.sh` implements.

- [ ] **Step 5.2.1: Write dispatch-abstraction.md**

```markdown
# RDF Dispatch Abstraction — Interface Specification

> Defines the contract for `lib/dispatch.sh` — the mode-aware agent dispatch
> layer that bridges subagent and Agent Teams execution models.

## 1. Design Principles

### 1.1 Instructions, Not Execution

The dispatch library generates **structured dispatch instructions** — it does
not make API calls. The mgr agent/command interprets these instructions and
makes the actual tool calls in its LLM context window.

Output format: structured markdown blocks that embed tool call parameters as
JSON. The mgr agent reads these blocks and translates them into tool calls.

### 1.2 Mode Transparency

Callers (mgr agent/command) use the same interface regardless of mode. The
dispatch output changes structure based on `RDF_AGENT_TEAMS` but the semantic
intent is identical: "spawn agent X with role Y, model Z, prompt P."

### 1.3 Zero Regression

When `RDF_AGENT_TEAMS=false` (default), output is functionally identical to
current hardcoded dispatch instructions in mgr.md. The abstraction replaces
inline dispatch blocks, not the dispatch behavior.

## 2. Feature Flag

**Environment variable:** `RDF_AGENT_TEAMS`
**Values:** `true` | `false` (default: `false`)
**Scope:** Read by `lib/dispatch.sh` at generation time, propagated into
generated mgr command/agent content.

When `rdf generate claude-code` runs:
- Reads `RDF_AGENT_TEAMS` from environment
- Passes it to the dispatch library
- Generated mgr command includes mode-appropriate dispatch blocks

Runtime override: The mgr agent can also check `RDF_AGENT_TEAMS` at dispatch
time and select the appropriate instruction set. This allows switching modes
without regenerating.

## 3. Agent Registry

File: `lib/dispatch-agents.json`

Maps agent roles to their properties across both dispatch modes.

```json
{
  "agents": {
    "sys-eng": {
      "role": "Senior Engineer",
      "canonical": "canonical/agents/sys-eng.md",
      "command": "canonical/commands/sys-eng.md",
      "model": "opus",
      "cc_name": "rfxn-sys-eng",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Write", "Edit", "Glob", "Grep"],
      "disallowed_tools": [],
      "can_write": true,
      "pipeline_stage": ["implementation"],
      "timeout_minutes": 10
    },
    "sys-qa": {
      "role": "QA Engineer",
      "canonical": "canonical/agents/sys-qa.md",
      "command": "canonical/commands/sys-qa.md",
      "model": "sonnet",
      "cc_name": "rfxn-sys-qa",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stage": ["verification"],
      "timeout_minutes": 10
    }
  }
}
```

Each agent entry contains everything needed to dispatch in either mode.

## 4. Task Templates

File: `lib/dispatch-tasks.json`

Defines pipeline stages as reusable task templates with dependency chains.

```json
{
  "templates": {
    "scope-validate": {
      "subject": "Scope: Validate Phase {phase}",
      "agent": "scope",
      "mode": "validate",
      "depends_on": [],
      "output_file": "work-output/scope-validation-{phase}.md"
    },
    "scope-workorder": {
      "subject": "Scope: Work Order for Phase {phase}",
      "agent": "scope",
      "mode": "workorder",
      "depends_on": [],
      "output_file": "work-output/scope-workorder-P{phase}.md"
    },
    "se-plan": {
      "subject": "SE: Plan Phase {phase}",
      "agent": "sys-eng",
      "mode": "plan-only",
      "depends_on": ["scope-workorder"],
      "output_file": "work-output/implementation-plan.md"
    },
    "challenger": {
      "subject": "Challenger: Review Phase {phase} Plan",
      "agent": "sys-challenger",
      "mode": "review",
      "depends_on": ["se-plan"],
      "output_file": "work-output/challenge-{phase}.md"
    },
    "se-implement": {
      "subject": "SE: Implement Phase {phase}",
      "agent": "sys-eng",
      "mode": "workorder",
      "depends_on": ["challenger"],
      "output_file": "work-output/phase-result.md"
    },
    "qa-gate": {
      "subject": "QA: Gate Phase {phase}",
      "agent": "sys-qa",
      "mode": "gate",
      "depends_on": ["se-implement"],
      "output_file": "work-output/qa-phase-{phase}-verdict.md"
    },
    "sentinel": {
      "subject": "Sentinel: Review Phase {phase}",
      "agent": "sys-sentinel",
      "mode": "standard",
      "depends_on": ["se-implement"],
      "output_file": "work-output/sentinel-{phase}.md"
    },
    "ux-review": {
      "subject": "UX Review: Phase {phase}",
      "agent": "sys-ux",
      "mode": "output-review",
      "depends_on": ["se-implement"],
      "output_file": "work-output/ux-review-{phase}.md"
    },
    "uat": {
      "subject": "UAT: Phase {phase}",
      "agent": "sys-uat",
      "mode": "standard",
      "depends_on": ["qa-gate"],
      "output_file": "work-output/uat-phase-{phase}-verdict.md"
    }
  }
}
```

## 5. Dispatch Function Interface

### rdf_dispatch_agent()

```
rdf_dispatch_agent <agent-name> <mode> <phase> <project-path> [extra-context]
```

Reads the agent registry, applies the feature flag, and outputs a structured
dispatch block.

**Subagent mode output:**
```markdown
### Dispatch: {role} ({agent-name})

**Tool:** Agent
**Parameters:**
- subagent_type: {cc_name} (fallback: {subagent_type} with model {model})
- prompt: "{generated_prompt}"
- timeout: {timeout_minutes} minutes

**Expected output:** {output_file}
```

**Agent Teams mode output:**
```markdown
### Dispatch: {role} ({agent-name})

**Tool:** Task (teammate)
**Parameters:**
- team_name: "rfxn-pipeline"
- name: "{agent-name}"
- subagent_type: "{team_agent_type}"
- model: "{model}"
- prompt: "{generated_prompt}"
- run_in_background: true

**Task:** {task_subject}
**Blocked by:** {dependency_list}
**Expected output:** {output_file}
```

### rdf_dispatch_pipeline()

```
rdf_dispatch_pipeline <tier> <phase> <project-path> [flags]
```

Generates the complete pipeline dispatch sequence for a given tier.

**Tier 0-1 output:** scope (optional) -> SE -> QA-lite
**Tier 2+ output:** scope -> SE-plan -> challenger -> SE-implement -> (QA + sentinel + ux-review parallel) -> UAT

In Agent Teams mode, this produces a complete TaskCreate sequence with
dependency chains, allowing the team to self-coordinate.

### rdf_dispatch_team_setup()

```
rdf_dispatch_team_setup <team-name> <description>
```

Agent Teams mode only. Generates TeamCreate instructions.

### rdf_dispatch_team_teardown()

```
rdf_dispatch_team_teardown <team-name>
```

Agent Teams mode only. Generates shutdown + TeamDelete instructions.

## 6. Prompt Generation

Each dispatch generates a role-specific prompt from a template. Templates
reference:
- The agent's canonical command file (`/root/.claude/commands/{name}.md`)
- The project CLAUDE.md path
- The parent CLAUDE.md path
- The work order path
- The output file path
- Mode-specific instructions (validate, workorder, gate, gate-lite, etc.)

Templates are stored in `lib/dispatch-tasks.json` as `prompt_template` fields,
with `{phase}`, `{project_path}`, `{output_file}` placeholders.

## 7. Adapter Integration

The Claude Code adapter gains optional Agent Teams configuration:

`adapters/claude-code/teams-meta.json`:
```json
{
  "team_name_prefix": "rfxn",
  "default_team_description": "rfxn engineering pipeline",
  "delegate_mode": true,
  "teammate_mode": "in-process",
  "hooks": {
    "TaskCompleted": {
      "script": "scripts/task-completed-gate.sh",
      "description": "Verify lint + test pass before task completion"
    },
    "TeammateIdle": {
      "script": "scripts/teammate-idle-handler.sh",
      "description": "Auto-assign next unblocked task"
    }
  },
  "settings": {
    "env": {
      "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
    },
    "teammateMode": "in-process"
  }
}
```

When `RDF_AGENT_TEAMS=true`, `rdf generate claude-code` includes Agent Teams
configuration in the generated output.

## 8. Migration Path

### Phase A: Abstraction (this phase)
- lib/dispatch.sh generates instructions
- mgr.md/mgr command updated to consume dispatch output
- Feature flag defaults to false
- No behavioral change for current users

### Phase B: Opt-in Testing (future)
- Set RDF_AGENT_TEAMS=true
- EM operates as team lead in delegate mode
- Test with tier 2+ phases (parallel QA + Sentinel benefits most)
- Compare token costs and wall time

### Phase C: Default Flip (future, after API stabilizes)
- Change default to true
- Subagent mode remains available via RDF_AGENT_TEAMS=false
- Document migration notes
```

- [ ] **Step 5.2.2: Commit specification**

```bash
git add docs/specs/dispatch-abstraction.md
git commit -m "$(cat <<'EOF'
Add dispatch abstraction interface specification

[New] docs/specs/dispatch-abstraction.md — interface contract for
  mode-aware agent dispatch (subagent vs Agent Teams)
[New] Agent registry schema, task template schema, function interfaces
[New] Prompt generation, adapter integration, migration path
EOF
)"
```

---

## Chunk 2: Agent Registry + Task Templates

### Task 5.3: Create Agent Registry

**Files:**
- Create: `lib/dispatch-agents.json`

The agent registry is the single source of truth for all dispatchable agents.
It replaces the hardcoded agent properties scattered through mgr.md.

- [ ] **Step 5.3.1: Write lib/dispatch-agents.json**

```json
{
  "_comment": "RDF dispatch agent registry — properties for subagent and Agent Teams modes",
  "_version": "1.0.0",
  "agents": {
    "scope": {
      "role": "Scoping and Research Agent",
      "canonical_agent": "canonical/agents/scope.md",
      "canonical_command": "canonical/commands/scope.md",
      "model": "sonnet",
      "cc_name": "rfxn-scope",
      "subagent_type": "general-purpose",
      "team_agent_type": "Explore",
      "tools": ["Read", "Glob", "Grep"],
      "disallowed_tools": ["Bash", "Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["scoping"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Scoping and Research Agent."
    },
    "sys-eng": {
      "role": "Senior Engineer",
      "canonical_agent": "canonical/agents/sys-eng.md",
      "canonical_command": "canonical/commands/sys-eng.md",
      "model": "opus",
      "cc_name": "rfxn-sys-eng",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Write", "Edit", "Glob", "Grep"],
      "disallowed_tools": [],
      "can_write": true,
      "pipeline_stages": ["implementation"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Senior Engineer."
    },
    "sys-qa": {
      "role": "QA Engineer",
      "canonical_agent": "canonical/agents/sys-qa.md",
      "canonical_command": "canonical/commands/sys-qa.md",
      "model": "sonnet",
      "cc_name": "rfxn-sys-qa",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the QA Engineer."
    },
    "sys-uat": {
      "role": "UAT Agent",
      "canonical_agent": "canonical/agents/sys-uat.md",
      "canonical_command": "canonical/commands/sys-uat.md",
      "model": "sonnet",
      "cc_name": "rfxn-sys-uat",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the UAT agent."
    },
    "sys-sentinel": {
      "role": "Sentinel (Post-Implementation Adversary)",
      "canonical_agent": "canonical/agents/sys-sentinel.md",
      "canonical_command": "canonical/commands/sys-sentinel.md",
      "model": "opus",
      "cc_name": "rfxn-sys-sentinel",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Sentinel agent."
    },
    "sys-challenger": {
      "role": "Challenger (Pre-Implementation Adversary)",
      "canonical_agent": "canonical/agents/sys-challenger.md",
      "canonical_command": "canonical/commands/sys-challenger.md",
      "model": "sonnet",
      "cc_name": "rfxn-sys-challenger",
      "subagent_type": "general-purpose",
      "team_agent_type": "Explore",
      "tools": ["Read", "Glob", "Grep"],
      "disallowed_tools": ["Bash", "Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["pre-implementation"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Challenger agent."
    },
    "sys-ux": {
      "role": "UX Reviewer",
      "canonical_agent": "canonical/agents/sys-ux.md",
      "canonical_command": "canonical/commands/sys-ux.md",
      "model": "sonnet",
      "cc_name": "rfxn-sys-ux",
      "subagent_type": "general-purpose",
      "team_agent_type": "Explore",
      "tools": ["Read", "Glob", "Grep"],
      "disallowed_tools": ["Bash", "Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the UX Reviewer."
    },
    "po": {
      "role": "Product Owner",
      "canonical_agent": "canonical/agents/po.md",
      "canonical_command": "canonical/commands/po.md",
      "model": "sonnet",
      "cc_name": "rfxn-po",
      "subagent_type": "general-purpose",
      "team_agent_type": "Explore",
      "tools": ["Read", "Glob", "Grep"],
      "disallowed_tools": ["Bash", "Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["intake"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Product Owner."
    },
    "sec-eng": {
      "role": "Security Engineer",
      "canonical_agent": "canonical/agents/sec-eng.md",
      "canonical_command": "canonical/commands/sec-eng.md",
      "model": "opus",
      "cc_name": "rfxn-sec-eng",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Write", "Edit", "Glob", "Grep"],
      "disallowed_tools": [],
      "can_write": true,
      "pipeline_stages": ["security"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Security Engineer."
    },
    "fe-qa": {
      "role": "Frontend QA",
      "canonical_agent": "canonical/agents/fe-qa.md",
      "canonical_command": "canonical/commands/fe-qa.md",
      "model": "sonnet",
      "cc_name": "rfxn-fe-qa",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Frontend QA Engineer."
    },
    "fe-uat": {
      "role": "Frontend UAT",
      "canonical_agent": "canonical/agents/fe-uat.md",
      "canonical_command": "canonical/commands/fe-uat.md",
      "model": "sonnet",
      "cc_name": "rfxn-fe-uat",
      "subagent_type": "general-purpose",
      "team_agent_type": "general-purpose",
      "tools": ["Bash", "Read", "Glob", "Grep"],
      "disallowed_tools": ["Write", "Edit"],
      "can_write": false,
      "pipeline_stages": ["verification"],
      "timeout_minutes": 10,
      "prompt_prefix": "You are the Frontend UAT agent."
    }
  }
}
```

- [ ] **Step 5.3.2: Validate JSON**

```bash
jq . lib/dispatch-agents.json >/dev/null && echo "valid"
```

---

### Task 5.4: Create Task Templates

**Files:**
- Create: `lib/dispatch-tasks.json`

Task templates define the pipeline stages with dependency chains and prompt
templates. Placeholder variables use `{var}` syntax for substitution.

- [ ] **Step 5.4.1: Write lib/dispatch-tasks.json**

```json
{
  "_comment": "RDF dispatch task templates — pipeline stages with dependency chains",
  "_version": "1.0.0",
  "team": {
    "name_template": "rfxn-{project}-p{phase}",
    "description_template": "rfxn pipeline: {project} phase {phase}"
  },
  "templates": {
    "scope-workorder": {
      "subject": "Scope: Work Order for Phase {phase}",
      "description": "Run workorder mode for phase {phase} at {project_path}. Write output to {output_file}.",
      "agent": "scope",
      "mode": "workorder",
      "depends_on": [],
      "output_file": "work-output/scope-workorder-P{phase}.md",
      "prompt_template": "You are the Scoping and Research Agent. Read `/root/.claude/commands/scope.md` for your full protocol. Run `workorder {phase}` for project at `{project_path}`. Read project PLAN file at `{plan_path}`. Create `./work-output/` if needed via mkdir. Write your work order draft to `./work-output/scope-workorder-P{phase}.md`.",
      "active_form": "Building work order for phase {phase}"
    },
    "scope-validate": {
      "subject": "Scope: Validate Phase {phase}",
      "description": "Run validate mode for phase {phase} at {project_path}. Write output to {output_file}.",
      "agent": "scope",
      "mode": "validate",
      "depends_on": [],
      "output_file": "work-output/scope-validation-{phase}.md",
      "prompt_template": "You are the Scoping and Research Agent. Read `/root/.claude/commands/scope.md` for your full protocol. Run `validate {phase}` for project at `{project_path}`. Read project PLAN file at `{plan_path}`. Create `./work-output/` if needed via mkdir. Write your validation report to `./work-output/scope-validation-{phase}.md`.",
      "active_form": "Validating scope for phase {phase}"
    },
    "se-plan": {
      "subject": "SE: Plan Phase {phase} (Steps 1-2)",
      "description": "Execute plan-only mode (Steps 1-2 only) for phase {phase}. Write implementation plan to work-output/implementation-plan.md.",
      "agent": "sys-eng",
      "mode": "plan-only",
      "depends_on": ["scope-workorder"],
      "output_file": "work-output/implementation-plan.md",
      "prompt_template": "You are the Senior Engineer. Read `/root/.claude/commands/sys-eng.md` for your full protocol. Execute in **plan-only** mode. Read project CLAUDE.md and parent CLAUDE.md at `/root/admin/work/proj/CLAUDE.md`. Create `./work-output/` if needed. Read the work order at `./work-output/current-phase.md`. Execute Steps 1-2 only. Write your implementation plan to `./work-output/implementation-plan.md`. Set STATUS: PLAN_COMPLETE in `./work-output/phase-{phase}-status.md`. Then STOP.",
      "active_form": "Planning implementation for phase {phase}"
    },
    "challenger": {
      "subject": "Challenger: Review Phase {phase} Plan",
      "description": "Review SE implementation plan for phase {phase}. Find design flaws, regressions, edge cases.",
      "agent": "sys-challenger",
      "mode": "review",
      "depends_on": ["se-plan"],
      "output_file": "work-output/challenge-{phase}.md",
      "prompt_template": "You are the Challenger agent. Read `/root/.claude/commands/sys-challenger.md` for your full protocol. Read `/root/admin/work/proj/CLAUDE.md` for project conventions. Review the SE's implementation plan at `./work-output/implementation-plan.md` for phase {phase}. Read the PLAN file at `{plan_path}` for the phase description. Read the relevant existing code that will be modified. Read MEMORY.md for lessons learned. Create `./work-output/` if needed. Write your findings to `./work-output/challenge-{phase}.md`.",
      "active_form": "Challenging phase {phase} design"
    },
    "se-implement": {
      "subject": "SE: Implement Phase {phase}",
      "description": "Execute 7-step SE protocol for phase {phase}. Full implementation.",
      "agent": "sys-eng",
      "mode": "workorder",
      "depends_on": ["challenger"],
      "output_file": "work-output/phase-result.md",
      "prompt_template": "You are the Senior Engineer. Read `/root/.claude/commands/sys-eng.md` for your full protocol. Execute the work order at `./work-output/current-phase.md`. Read project CLAUDE.md and parent CLAUDE.md at `/root/admin/work/proj/CLAUDE.md` before starting. Create `./work-output/` if needed. Write status updates to `./work-output/phase-{phase}-status.md` at each step. Write results to `./work-output/phase-result.md` when done.",
      "active_form": "Implementing phase {phase}"
    },
    "se-implement-direct": {
      "subject": "SE: Implement Phase {phase} (no challenger)",
      "description": "Execute 7-step SE protocol for phase {phase}. Tier 0-1, no challenger gate.",
      "agent": "sys-eng",
      "mode": "workorder",
      "depends_on": ["scope-workorder"],
      "output_file": "work-output/phase-result.md",
      "prompt_template": "You are the Senior Engineer. Read `/root/.claude/commands/sys-eng.md` for your full protocol. Execute the work order at `./work-output/current-phase.md`. Read project CLAUDE.md and parent CLAUDE.md at `/root/admin/work/proj/CLAUDE.md` before starting. Create `./work-output/` if needed. Write status updates to `./work-output/phase-{phase}-status.md` at each step. Write results to `./work-output/phase-result.md` when done.",
      "active_form": "Implementing phase {phase}"
    },
    "qa-gate": {
      "subject": "QA: Full Gate Phase {phase}",
      "description": "Run full QA gate for phase {phase}. Read SE result and Sentinel findings.",
      "agent": "sys-qa",
      "mode": "gate",
      "depends_on": ["se-implement"],
      "output_file": "work-output/qa-phase-{phase}-verdict.md",
      "prompt_template": "You are the QA Engineer. Read `/root/.claude/commands/sys-qa.md` for your full protocol. Run gate mode for the current branch. Read the SE result at `./work-output/phase-result.md` for context. Create `./work-output/` if needed. Write status updates to `./work-output/qa-phase-{phase}-status.md` at each step. Write your verdict to `./work-output/qa-phase-{phase}-verdict.md`.",
      "active_form": "QA gate for phase {phase}"
    },
    "qa-gate-lite": {
      "subject": "QA: Lite Gate Phase {phase}",
      "description": "Run lite QA gate for phase {phase} (tier 0-1).",
      "agent": "sys-qa",
      "mode": "gate-lite",
      "depends_on": ["se-implement-direct"],
      "output_file": "work-output/qa-phase-{phase}-verdict.md",
      "prompt_template": "You are the QA Engineer. Read `/root/.claude/commands/sys-qa.md` for your full protocol. Run **gate-lite** mode for the current branch. Read the SE result at `./work-output/phase-result.md` for context. Create `./work-output/` if needed. Write status updates to `./work-output/qa-phase-{phase}-status.md` at each step. Write your verdict to `./work-output/qa-phase-{phase}-verdict.md`.",
      "active_form": "QA lite gate for phase {phase}"
    },
    "sentinel": {
      "subject": "Sentinel: Review Phase {phase}",
      "description": "Run 4-pass adversarial review on SE diff for phase {phase}.",
      "agent": "sys-sentinel",
      "mode": "standard",
      "depends_on": ["se-implement"],
      "output_file": "work-output/sentinel-{phase}.md",
      "prompt_template": "You are the Sentinel agent. Read `/root/.claude/commands/sys-sentinel.md` for your full protocol. Read `/root/admin/work/proj/CLAUDE.md` for project conventions. Run four adversarial passes on the diff for phase {phase} in project at `{project_path}`. Read the SE result at `./work-output/phase-result.md` for context. Create `./work-output/` if needed via `mkdir -p ./work-output`. Write your findings to `./work-output/sentinel-{phase}.md`.",
      "active_form": "Sentinel review for phase {phase}"
    },
    "ux-review": {
      "subject": "UX Review: Phase {phase}",
      "description": "Review output surface changes for phase {phase}.",
      "agent": "sys-ux",
      "mode": "output-review",
      "depends_on": ["se-implement"],
      "output_file": "work-output/ux-review-{phase}.md",
      "prompt_template": "You are the UX Reviewer. Read `/root/.claude/commands/sys-ux.md` for your full protocol. Read `/root/admin/work/proj/CLAUDE.md` for project conventions. Run OUTPUT_REVIEW mode for phase {phase} in project at `{project_path}`. Read the SE result at `./work-output/phase-result.md` for context. Read the Design System Reference at `/root/admin/work/proj/reference/design-system.md`. Create `./work-output/` if needed via `mkdir -p ./work-output`. Write your findings to `./work-output/ux-review-{phase}.md`.",
      "active_form": "UX review for phase {phase}"
    },
    "uat": {
      "subject": "UAT: Phase {phase}",
      "description": "Run user acceptance testing for phase {phase}.",
      "agent": "sys-uat",
      "mode": "standard",
      "depends_on": ["qa-gate"],
      "output_file": "work-output/uat-phase-{phase}-verdict.md",
      "prompt_template": "You are the UAT agent. Read `/root/.claude/commands/sys-uat.md` for your full protocol. Run UAT for phase {phase} in project at `{project_path}`. Read the SE result at `./work-output/phase-result.md` for context. Create `./work-output/` if needed. Write status updates to `./work-output/uat-phase-{phase}-status.md` at each step. Write your verdict to `./work-output/uat-phase-{phase}-verdict.md`.",
      "active_form": "UAT for phase {phase}"
    }
  },
  "pipelines": {
    "tier-0-1": {
      "description": "Lightweight pipeline for docs-only and single-scope changes",
      "stages": ["scope-workorder", "se-implement-direct", "qa-gate-lite"],
      "optional_stages": ["ux-review"]
    },
    "tier-2-plus": {
      "description": "Full pipeline for multi-file core, install scripts, shared libs",
      "stages": ["scope-workorder", "se-plan", "challenger", "se-implement", "qa-gate", "sentinel", "uat"],
      "optional_stages": ["ux-review"],
      "parallel_groups": {
        "verification": ["qa-gate", "sentinel", "ux-review"]
      }
    },
    "tier-2-no-challenger": {
      "description": "Full pipeline with challenger skipped",
      "stages": ["scope-workorder", "se-implement", "qa-gate", "sentinel", "uat"],
      "optional_stages": ["ux-review"],
      "parallel_groups": {
        "verification": ["qa-gate", "sentinel", "ux-review"]
      }
    }
  }
}
```

- [ ] **Step 5.4.2: Validate JSON**

```bash
jq . lib/dispatch-tasks.json >/dev/null && echo "valid"
```

- [ ] **Step 5.4.3: Commit registry and templates**

```bash
git add lib/dispatch-agents.json lib/dispatch-tasks.json
git commit -m "$(cat <<'EOF'
Add dispatch agent registry and task templates

[New] lib/dispatch-agents.json — centralized agent properties for both
  subagent and Agent Teams dispatch modes (11 agents)
[New] lib/dispatch-tasks.json — pipeline stage templates with dependency
  chains, prompt templates, and tier-based pipeline definitions
EOF
)"
```

---

## Chunk 3: Dispatch Library Implementation

### Task 5.5: Write lib/dispatch.sh

**Files:**
- Create: `lib/dispatch.sh`
- Modify: `lib/rdf_common.sh` (add feature flag helper)

The dispatch library is the core of Phase 5. It reads the agent registry and
task templates, applies the feature flag, and outputs structured dispatch
instructions as markdown.

- [ ] **Step 5.5.1: Add feature flag helper to lib/rdf_common.sh**

Add after the existing `rdf_require_dir()` function:

```bash
# Feature flags — environment-based
rdf_feature_enabled() {
    local flag="$1"
    local val
    val="${!flag:-false}"
    [[ "$val" == "true" || "$val" == "1" ]]
}
```

- [ ] **Step 5.5.2: Write lib/dispatch.sh**

```bash
#!/usr/bin/env bash
# lib/dispatch.sh — Mode-aware agent dispatch abstraction
# (C) 2026 R-fx Networks <proj@rfxn.com>
# GNU GPL v2
# Sourced by lib/cmd/dispatch.sh or adapters — do not execute directly

[[ -n "${_RDF_DISPATCH_LOADED:-}" ]] && return 0 2>/dev/null
_RDF_DISPATCH_LOADED=1

# Requires rdf_common.sh to be loaded first
if [[ -z "${_RDF_COMMON_LOADED:-}" ]]; then
    echo "dispatch.sh: fatal: rdf_common.sh must be loaded first" >&2
    exit 1
fi

# --- Configuration ---
RDF_DISPATCH_AGENTS="${RDF_HOME}/lib/dispatch-agents.json"
RDF_DISPATCH_TASKS="${RDF_HOME}/lib/dispatch-tasks.json"

# --- Internal helpers ---

# Substitute {var} placeholders in a string
# Usage: _dispatch_subst "template" "var1=val1" "var2=val2" ...
_dispatch_subst() {
    local template="$1"
    shift
    local kv key val
    for kv in "$@"; do
        key="${kv%%=*}"
        val="${kv#*=}"
        # Use a variable to avoid bash 4.x ${var/pat/repl} brace trap
        template="${template//\{${key}\}/${val}}"
    done
    printf '%s' "$template"
}

# Read a field from the agent registry
# Usage: _dispatch_agent_field <agent-name> <field>
_dispatch_agent_field() {
    local agent="$1"
    local field="$2"
    rdf_require_file "$RDF_DISPATCH_AGENTS" "dispatch agent registry"
    jq -r ".agents[\"${agent}\"].${field} // empty" "$RDF_DISPATCH_AGENTS"
}

# Read a field from a task template
# Usage: _dispatch_task_field <template-name> <field>
_dispatch_task_field() {
    local tmpl="$1"
    local field="$2"
    rdf_require_file "$RDF_DISPATCH_TASKS" "dispatch task templates"
    jq -r ".templates[\"${tmpl}\"].${field} // empty" "$RDF_DISPATCH_TASKS"
}

# Read the pipeline definition for a tier
# Usage: _dispatch_pipeline_stages <pipeline-name>
_dispatch_pipeline_stages() {
    local pipeline="$1"
    rdf_require_file "$RDF_DISPATCH_TASKS" "dispatch task templates"
    jq -r ".pipelines[\"${pipeline}\"].stages[]" "$RDF_DISPATCH_TASKS"
}

# Check if Agent Teams mode is enabled
_dispatch_is_teams_mode() {
    rdf_feature_enabled "RDF_AGENT_TEAMS"
}

# --- Public API ---

# Generate dispatch instructions for a single agent
# Usage: rdf_dispatch_agent <agent-name> <task-template> <phase> <project-path> <plan-path>
# Output: Structured markdown dispatch block to stdout
rdf_dispatch_agent() {
    local agent_name="$1"
    local task_tmpl="$2"
    local phase="$3"
    local project_path="$4"
    local plan_path="${5:-}"

    rdf_require_bin jq

    # Resolve agent properties
    local role model cc_name subagent_type team_agent_type timeout
    role="$(_dispatch_agent_field "$agent_name" "role")"
    model="$(_dispatch_agent_field "$agent_name" "model")"
    cc_name="$(_dispatch_agent_field "$agent_name" "cc_name")"
    subagent_type="$(_dispatch_agent_field "$agent_name" "subagent_type")"
    team_agent_type="$(_dispatch_agent_field "$agent_name" "team_agent_type")"
    timeout="$(_dispatch_agent_field "$agent_name" "timeout_minutes")"

    if [[ -z "$role" ]]; then
        rdf_die "unknown agent: $agent_name (not in dispatch registry)"
    fi

    # Resolve task template properties
    local subject output_file prompt_template active_form
    local raw_subject raw_output raw_prompt raw_active
    raw_subject="$(_dispatch_task_field "$task_tmpl" "subject")"
    raw_output="$(_dispatch_task_field "$task_tmpl" "output_file")"
    raw_prompt="$(_dispatch_task_field "$task_tmpl" "prompt_template")"
    raw_active="$(_dispatch_task_field "$task_tmpl" "active_form")"

    # Substitute placeholders
    local subs=("phase=${phase}" "project_path=${project_path}" "plan_path=${plan_path}")
    subject="$(_dispatch_subst "$raw_subject" "${subs[@]}")"
    output_file="$(_dispatch_subst "$raw_output" "${subs[@]}")"
    local prompt
    prompt="$(_dispatch_subst "$raw_prompt" "${subs[@]}")"
    active_form="$(_dispatch_subst "$raw_active" "${subs[@]}")"

    # Resolve dependencies
    local deps_json deps_list
    deps_json="$(jq -r ".templates[\"${task_tmpl}\"].depends_on // [] | join(\", \")" "$RDF_DISPATCH_TASKS")"

    if _dispatch_is_teams_mode; then
        _dispatch_emit_teams "$agent_name" "$role" "$model" "$cc_name" \
            "$team_agent_type" "$subject" "$prompt" "$output_file" \
            "$active_form" "$deps_json" "$timeout"
    else
        _dispatch_emit_subagent "$agent_name" "$role" "$model" "$cc_name" \
            "$subagent_type" "$prompt" "$output_file" "$timeout"
    fi
}

# Generate subagent mode dispatch block
_dispatch_emit_subagent() {
    local agent_name="$1" role="$2" model="$3" cc_name="$4"
    local subagent_type="$5" prompt="$6" output_file="$7" timeout="$8"

    cat <<DISPATCH_BLOCK

### Dispatch: ${role} (${agent_name})

**Mode:** Subagent
**Tool:** Agent
**Parameters:**
- subagent_type: \`${cc_name}\` (fallback: \`${subagent_type}\` with model \`${model}\`)
- prompt: see below
- timeout: ${timeout} minutes

**Prompt:**
> ${prompt}

**Expected output:** \`${output_file}\`

DISPATCH_BLOCK
}

# Generate Agent Teams mode dispatch block
_dispatch_emit_teams() {
    local agent_name="$1" role="$2" model="$3" cc_name="$4"
    local team_agent_type="$5" subject="$6" prompt="$7" output_file="$8"
    local active_form="$9" deps="${10}" timeout="${11}"

    cat <<DISPATCH_BLOCK

### Dispatch: ${role} (${agent_name})

**Mode:** Agent Teams (teammate)
**Tools:** TaskCreate + Task (teammate spawn)

**TaskCreate parameters:**
\`\`\`json
{
  "subject": "${subject}",
  "description": "${prompt}",
  "activeForm": "${active_form}"
}
\`\`\`

**Task (teammate) parameters:**
\`\`\`json
{
  "team_name": "{team_name}",
  "name": "${agent_name}",
  "subagent_type": "${team_agent_type}",
  "model": "${model}",
  "prompt": "${prompt}",
  "run_in_background": true
}
\`\`\`

**Blocked by:** ${deps:-none}
**Expected output:** \`${output_file}\`
**Timeout:** ${timeout} minutes

DISPATCH_BLOCK
}

# Generate a complete pipeline dispatch sequence
# Usage: rdf_dispatch_pipeline <tier> <phase> <project-path> <plan-path> [flags...]
# Flags: --no-challenger, --no-ux, --no-uat, --no-scope
# Output: Complete pipeline dispatch instructions to stdout
rdf_dispatch_pipeline() {
    local tier="$1"
    local phase="$2"
    local project_path="$3"
    local plan_path="$4"
    shift 4

    # Parse flags
    local no_challenger="" no_ux="" no_uat="" no_scope=""
    local arg
    for arg in "$@"; do
        case "$arg" in
            --no-challenger) no_challenger=1 ;;
            --no-ux)         no_ux=1 ;;
            --no-uat)        no_uat=1 ;;
            --no-scope)      no_scope=1 ;;
        esac
    done

    rdf_require_bin jq

    # Select pipeline based on tier
    local pipeline_name
    if [[ "$tier" -le 1 ]]; then
        pipeline_name="tier-0-1"
    elif [[ -n "$no_challenger" ]]; then
        pipeline_name="tier-2-no-challenger"
    else
        pipeline_name="tier-2-plus"
    fi

    # Header
    local mode_label="Subagent"
    if _dispatch_is_teams_mode; then
        mode_label="Agent Teams"
    fi

    cat <<PIPELINE_HEADER
## Pipeline Dispatch: Phase ${phase} (Tier ${tier})

**Mode:** ${mode_label}
**Project:** ${project_path}
**Plan:** ${plan_path}

PIPELINE_HEADER

    # Agent Teams: emit team setup
    if _dispatch_is_teams_mode; then
        local team_name_tmpl team_desc_tmpl team_name team_desc
        team_name_tmpl="$(jq -r '.team.name_template' "$RDF_DISPATCH_TASKS")"
        team_desc_tmpl="$(jq -r '.team.description_template' "$RDF_DISPATCH_TASKS")"
        local project_basename
        project_basename="$(basename "$project_path")"
        team_name="$(_dispatch_subst "$team_name_tmpl" "project=${project_basename}" "phase=${phase}")"
        team_desc="$(_dispatch_subst "$team_desc_tmpl" "project=${project_basename}" "phase=${phase}")"

        cat <<TEAM_SETUP
### Team Setup

**Tool:** TeamCreate
\`\`\`json
{
  "team_name": "${team_name}",
  "description": "${team_desc}"
}
\`\`\`

---

TEAM_SETUP
    fi

    # Emit each stage
    local stages stage agent_name
    stages="$(_dispatch_pipeline_stages "$pipeline_name")"

    while IFS= read -r stage; do
        [[ -z "$stage" ]] && continue

        # Apply skip flags
        case "$stage" in
            scope-*) [[ -n "$no_scope" ]] && continue ;;
            challenger) [[ -n "$no_challenger" ]] && continue ;;
            ux-review) [[ -n "$no_ux" ]] && continue ;;
            uat) [[ -n "$no_uat" ]] && continue ;;
        esac

        agent_name="$(_dispatch_task_field "$stage" "agent")"
        rdf_dispatch_agent "$agent_name" "$stage" "$phase" "$project_path" "$plan_path"
        echo "---"
    done <<< "$stages"

    # Agent Teams: emit parallel group note
    if _dispatch_is_teams_mode; then
        local parallel_group
        parallel_group="$(jq -r ".pipelines[\"${pipeline_name}\"].parallel_groups // {} | keys[]" "$RDF_DISPATCH_TASKS" 2>/dev/null)" || true  # no parallel groups is fine
        if [[ -n "$parallel_group" ]]; then
            echo ""
            echo "### Parallel Execution Groups"
            echo ""
            local group_stages
            while IFS= read -r group_name; do
                group_stages="$(jq -r ".pipelines[\"${pipeline_name}\"].parallel_groups[\"${group_name}\"] | join(\", \")" "$RDF_DISPATCH_TASKS")"
                echo "- **${group_name}:** ${group_stages}"
            done <<< "$parallel_group"
            echo ""
            echo "In Agent Teams mode, tasks in parallel groups share the same"
            echo "dependency set and execute concurrently via self-claiming."
        fi
    fi

    # Agent Teams: emit teardown
    if _dispatch_is_teams_mode; then
        cat <<TEAM_TEARDOWN

---

### Team Teardown

After all tasks complete and results are collected:

1. Send shutdown_request to all teammates
2. Wait for shutdown_response from each
3. Run TeamDelete to clean up

TEAM_TEARDOWN
    fi
}

# Show dispatch mode status
# Usage: rdf_dispatch_status
rdf_dispatch_status() {
    local mode="subagent"
    if _dispatch_is_teams_mode; then
        mode="agent-teams"
    fi
    local agent_count
    agent_count="$(jq '.agents | length' "$RDF_DISPATCH_AGENTS")"
    local template_count
    template_count="$(jq '.templates | length' "$RDF_DISPATCH_TASKS")"
    local pipeline_count
    pipeline_count="$(jq '.pipelines | length' "$RDF_DISPATCH_TASKS")"

    cat <<STATUS
Dispatch Mode: ${mode}
Feature Flag:  RDF_AGENT_TEAMS=${RDF_AGENT_TEAMS:-false}
Agents:        ${agent_count}
Templates:     ${template_count}
Pipelines:     ${pipeline_count}
STATUS
}
```

- [ ] **Step 5.5.3: Verify syntax**

```bash
bash -n lib/dispatch.sh
shellcheck lib/dispatch.sh
```

- [ ] **Step 5.5.4: Add dispatch subcommand handler**

Create `lib/cmd/dispatch.sh`:

```bash
#!/usr/bin/env bash
# lib/cmd/dispatch.sh — rdf dispatch subcommand
# (C) 2026 R-fx Networks <proj@rfxn.com>
# GNU GPL v2

# shellcheck disable=SC1090
source "${RDF_LIBDIR}/dispatch.sh"

cmd_dispatch() {
    local subcmd="${1:-help}"
    shift 2>/dev/null || true  # shift may fail if no args — safe to ignore

    case "$subcmd" in
        agent)
            # rdf dispatch agent <name> <task-template> <phase> <project-path> [plan-path]
            if [[ $# -lt 4 ]]; then
                rdf_die "usage: rdf dispatch agent <name> <template> <phase> <project-path> [plan-path]"
            fi
            rdf_dispatch_agent "$@"
            ;;
        pipeline)
            # rdf dispatch pipeline <tier> <phase> <project-path> <plan-path> [flags...]
            if [[ $# -lt 4 ]]; then
                rdf_die "usage: rdf dispatch pipeline <tier> <phase> <project-path> <plan-path> [flags...]"
            fi
            rdf_dispatch_pipeline "$@"
            ;;
        status)
            rdf_dispatch_status
            ;;
        help|--help|-h)
            cat <<USAGE
Usage: rdf dispatch <subcommand> [options]

Generate mode-aware agent dispatch instructions.

Subcommands:
  agent     Generate dispatch for a single agent
  pipeline  Generate full pipeline dispatch sequence
  status    Show current dispatch mode and registry stats

Dispatch mode is controlled by RDF_AGENT_TEAMS environment variable:
  false (default) — subagent mode (Agent tool)
  true            — Agent Teams mode (TeammateTool)

Examples:
  rdf dispatch status
  rdf dispatch agent sys-eng se-implement 3 /path/to/project /path/to/PLAN.md
  rdf dispatch pipeline 2 3 /path/to/project /path/to/PLAN.md
  rdf dispatch pipeline 2 3 /path/to/project /path/to/PLAN.md --no-challenger
  RDF_AGENT_TEAMS=true rdf dispatch pipeline 2 3 /path/to/project /path/to/PLAN.md
USAGE
            ;;
        *)
            rdf_die "unknown dispatch subcommand: $subcmd — run 'rdf dispatch help'"
            ;;
    esac
}
```

- [ ] **Step 5.5.5: Register dispatch subcommand in bin/rdf**

Add to the case statement in `bin/rdf`, after the `state` line:

```bash
    dispatch) shift; source "${RDF_LIBDIR}/cmd/dispatch.sh"; cmd_dispatch "$@" ;;
```

Update the usage function to include:
```
  dispatch   Generate mode-aware agent dispatch instructions
```

- [ ] **Step 5.5.6: Verify syntax for all new/modified files**

```bash
bash -n lib/dispatch.sh
bash -n lib/cmd/dispatch.sh
bash -n bin/rdf
shellcheck lib/dispatch.sh
shellcheck lib/cmd/dispatch.sh
shellcheck bin/rdf
```

- [ ] **Step 5.5.7: Functional test — subagent mode**

```bash
# Default mode (subagent)
bin/rdf dispatch status
# Expect: Dispatch Mode: subagent

bin/rdf dispatch agent sys-eng se-implement 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md
# Expect: Subagent dispatch block with Agent tool parameters

bin/rdf dispatch pipeline 0 1 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md
# Expect: Tier 0-1 pipeline (scope -> SE -> QA-lite)

bin/rdf dispatch pipeline 2 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md
# Expect: Tier 2+ pipeline (scope -> SE-plan -> challenger -> SE -> QA+sentinel+ux -> UAT)
```

- [ ] **Step 5.5.8: Functional test — Agent Teams mode**

```bash
# Teams mode
RDF_AGENT_TEAMS=true bin/rdf dispatch status
# Expect: Dispatch Mode: agent-teams

RDF_AGENT_TEAMS=true bin/rdf dispatch agent sys-eng se-implement 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md
# Expect: Agent Teams dispatch block with TaskCreate + Task parameters

RDF_AGENT_TEAMS=true bin/rdf dispatch pipeline 2 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md
# Expect: Teams pipeline with TeamCreate, dependency chains, parallel groups, teardown
```

- [ ] **Step 5.5.9: Commit dispatch library**

```bash
git add lib/dispatch.sh lib/cmd/dispatch.sh lib/rdf_common.sh bin/rdf
git commit -m "$(cat <<'EOF'
Implement dispatch abstraction library with dual-mode support

[New] lib/dispatch.sh — mode-aware agent dispatch (subagent vs Agent Teams)
[New] lib/cmd/dispatch.sh — rdf dispatch subcommand handler
[Change] lib/rdf_common.sh — add rdf_feature_enabled() for feature flags
[Change] bin/rdf — register dispatch subcommand
[New] rdf dispatch agent/pipeline/status CLI interface
[New] RDF_AGENT_TEAMS feature flag (default: false)
EOF
)"
```

---

## Chunk 4: Adapter Integration + Teams Metadata

### Task 5.6: Create Agent Teams Adapter Metadata

**Files:**
- Create: `adapters/claude-code/teams-meta.json`

This file contains CC-specific Agent Teams configuration that `rdf generate
claude-code` uses when `RDF_AGENT_TEAMS=true`.

- [ ] **Step 5.6.1: Write teams-meta.json**

```json
{
  "_comment": "Claude Code Agent Teams configuration — used by rdf generate when RDF_AGENT_TEAMS=true",
  "_version": "1.0.0",
  "enable_setting": {
    "key": "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS",
    "value": "1",
    "location": "settings.json env block"
  },
  "team_defaults": {
    "name_prefix": "rfxn",
    "delegate_mode": true,
    "teammate_mode": "in-process"
  },
  "agent_type_mapping": {
    "_comment": "Maps RDF agent types to CC Agent Teams agent types",
    "general-purpose": "general-purpose",
    "read-only": "Explore",
    "plan-only": "Plan",
    "shell-only": "Bash"
  },
  "hooks": {
    "TaskCompleted": {
      "command": "scripts/task-completed-gate.sh",
      "description": "Verify lint + test pass before task completion",
      "exit_code_reject": 2
    },
    "TeammateIdle": {
      "command": "scripts/teammate-idle-handler.sh",
      "description": "Auto-assign next unblocked task or graceful shutdown",
      "exit_code_redirect": 2
    }
  },
  "model_preferences": {
    "_comment": "Cost-optimized model assignment for Agent Teams mode",
    "lead": "sonnet",
    "semantic_depth": "opus",
    "focused_work": "sonnet",
    "checklist": "haiku"
  },
  "settings_fragment": {
    "env": {
      "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
    },
    "teammateMode": "in-process"
  }
}
```

- [ ] **Step 5.6.2: Validate JSON**

```bash
jq . adapters/claude-code/teams-meta.json >/dev/null && echo "valid"
```

- [ ] **Step 5.6.3: Commit adapter metadata**

```bash
git add adapters/claude-code/teams-meta.json
git commit -m "$(cat <<'EOF'
Add Agent Teams adapter metadata for Claude Code

[New] adapters/claude-code/teams-meta.json — CC-specific Agent Teams
  configuration (hooks, model preferences, settings fragment)
EOF
)"
```

---

## Chunk 5: Agent Updates (mgr.md + mgr command)

### Task 5.7: Update mgr agent for dispatch awareness

**Files:**
- Modify: `canonical/agents/mgr.md`

The mgr agent gains awareness of the dispatch abstraction. It references
`rdf dispatch` as the source of dispatch instructions rather than hardcoded
tool call parameters.

- [ ] **Step 5.7.1: Update canonical/agents/mgr.md**

Add a new section after "Dispatch Reference":

```markdown
## Dispatch Modes

RDF supports two agent dispatch modes, controlled by the `RDF_AGENT_TEAMS`
environment variable:

### Subagent Mode (default, RDF_AGENT_TEAMS=false)
- Each agent is spawned via the Agent tool as an isolated subagent
- Results return to EM's context via work-output/ files
- Agents cannot communicate with each other
- Lower token cost, simpler coordination
- This is the current production mode

### Agent Teams Mode (RDF_AGENT_TEAMS=true)
- EM operates as team lead in delegate mode (coordination only)
- Agents are spawned as teammates with shared task list
- Teammates can message each other directly
- Dependencies are managed via task blocking
- Higher token cost, better for parallel exploration
- Requires Claude Code v2.1.32+ with CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1

### Dispatch Instructions
When dispatching agents, use the instructions from `rdf dispatch pipeline`
or `rdf dispatch agent` for the current mode. The dispatch abstraction
generates mode-appropriate tool call parameters.

Run `rdf dispatch status` to check the current mode.

### Mode-Specific Behavior
- **Subagent mode:** Dispatch agents sequentially or in parallel via
  multiple Agent tool calls in the same message. Read work-output/ files
  for results.
- **Agent Teams mode:** Create team via TeamCreate, define tasks via
  TaskCreate with dependency chains, spawn teammates via Task. Teammates
  self-claim unblocked tasks. Use SendMessage for coordination. Read
  work-output/ files AND inbox messages for results.
```

- [ ] **Step 5.7.2: Verify no frontmatter in canonical agent**

```bash
head -1 canonical/agents/mgr.md
# Expect: plain text (no "---" YAML frontmatter marker)
```

---

### Task 5.8: Update mgr command for dispatch abstraction

**Files:**
- Modify: `canonical/commands/mgr.md`

This is the larger change. The mgr command's dispatch sections (Steps 3, 3.5,
5, 7) gain references to the dispatch abstraction as an alternative to the
current hardcoded instructions. The current hardcoded instructions are preserved
as the default path (subagent mode).

- [ ] **Step 5.8.1: Add dispatch abstraction reference**

Add a new section near the top of the mgr command (after "## Arguments"):

```markdown
## Dispatch Abstraction

This command supports two dispatch modes. The default (subagent) mode uses
the hardcoded dispatch instructions below. When `RDF_AGENT_TEAMS=true`, use
the dispatch abstraction instead:

```
# Check current mode
rdf dispatch status

# Generate single agent dispatch
rdf dispatch agent <name> <template> <phase> <project-path> [plan-path]

# Generate complete pipeline
rdf dispatch pipeline <tier> <phase> <project-path> <plan-path> [flags]
```

The dispatch abstraction produces mode-appropriate instructions. In subagent
mode, its output matches the hardcoded instructions below. In Agent Teams
mode, it produces TeamCreate/TaskCreate/Task instructions with dependency
chains.

**Current mode:** Subagent (default). The hardcoded instructions below are
the authoritative dispatch protocol. The dispatch abstraction is available
for Agent Teams mode or for tooling that consumes structured dispatch data.
```

- [ ] **Step 5.8.2: Verify the command file is well-formed**

The changes are additive — all existing dispatch instructions remain unchanged.
Verify the file still parses correctly as markdown.

```bash
wc -l canonical/commands/mgr.md
# Record line count for regression check
```

- [ ] **Step 5.8.3: Commit agent updates**

```bash
git add canonical/agents/mgr.md canonical/commands/mgr.md
git commit -m "$(cat <<'EOF'
Update mgr agent and command for dispatch abstraction

[New] canonical/agents/mgr.md — dispatch modes section with dual-mode
  awareness (subagent vs Agent Teams)
[New] canonical/commands/mgr.md — dispatch abstraction reference section
  with rdf dispatch CLI integration
[Change] Existing hardcoded dispatch instructions preserved as default
EOF
)"
```

---

## Chunk 6: Task-Based Coordination Specification

### Task 5.9: Design Task-Based Coordination Spec

**Files:**
- Create: `docs/specs/task-based-coordination.md`

This document maps the current file-based coordination model (work-output/
files) to a task-based model (Agent Teams shared task list + messaging).

- [ ] **Step 5.9.1: Write task-based-coordination.md**

```markdown
# Task-Based Coordination Specification

> Maps the current file-based coordination model to Agent Teams task-based
> coordination. Ensures zero regression when switching between modes.

## 1. Current Model: File-Based Coordination

### Communication Channel
- **EM -> Agent:** Work order file (`work-output/current-phase.md`) + prompt
- **Agent -> EM:** Result files (`work-output/phase-result.md`, etc.)
- **Agent -> Agent:** None (isolated subagents cannot communicate)

### Coordination Mechanism
- **Sequencing:** EM dispatches agents one at a time (or parallel via
  multiple Agent tool calls)
- **Dependencies:** EM manages manually — waits for result file before
  dispatching next agent
- **Status tracking:** EM reads status files between dispatches

### File Contract

| Stage | Input Files | Output Files |
|-------|-------------|-------------|
| Scope | PLAN.md | scope-workorder-P{N}.md or scope-validation-{N}.md |
| SE (plan) | current-phase.md | implementation-plan.md, phase-{N}-status.md |
| Challenger | implementation-plan.md | challenge-{N}.md |
| SE (impl) | current-phase.md | phase-result.md, phase-{N}-status.md |
| QA | phase-result.md | qa-phase-{N}-status.md, qa-phase-{N}-verdict.md |
| Sentinel | phase-result.md | sentinel-{N}.md |
| UX Review | phase-result.md | ux-review-{N}.md |
| UAT | phase-result.md | uat-phase-{N}-status.md, uat-phase-{N}-verdict.md |

## 2. New Model: Task-Based Coordination

### Communication Channel
- **EM -> Agent:** TaskCreate (subject + description) + spawn prompt
- **Agent -> EM:** TaskUpdate (completed) + SendMessage (summary)
- **Agent -> Agent:** SendMessage (direct, e.g., QA reads Sentinel findings)

### Coordination Mechanism
- **Sequencing:** TaskCreate with `addBlockedBy` creates dependency chains
- **Dependencies:** Automatic — tasks unblock when predecessors complete
- **Status tracking:** TaskList shows real-time status of all work

### Task Dependency Graph (Tier 2+ Pipeline)

```
[scope-workorder]
       |
  [se-plan] ───────────────────────────
       |                               |
  [challenger]                         |
       |                               |
  [se-implement]                       |
       |                               |
  +----+----+----+                     |
  |         |    |                     |
[qa-gate] [sentinel] [ux-review]       |
  |                                    |
  [uat]                                |
```

### Mapping Table

| File-Based | Task-Based | Notes |
|------------|-----------|-------|
| Write work order file | TaskCreate + prompt | Task description replaces work order |
| Read result file | TaskList + SendMessage | Teammate sends summary on completion |
| Parallel Agent calls | Tasks with same depends_on | Self-claiming handles parallelism |
| EM waits for result | Task auto-unblocks downstream | No polling needed |
| EM reads + decides | Lead reviews task summaries | Delegate mode enforces coordination-only |

## 3. Dual-Mode Invariants

These properties MUST hold in both modes:

1. **Output files are always written.** Even in Agent Teams mode, agents
   write to work-output/ files. This ensures:
   - Post-pipeline analysis works identically
   - Pipeline metrics collection is mode-independent
   - Debugging uses the same file artifacts
   - Fallback to subagent mode loses no data

2. **Prompt content is identical.** The dispatch abstraction generates the
   same prompt text regardless of mode. Only the dispatch mechanism changes.

3. **Pipeline ordering is preserved.** Dependencies enforce the same
   execution order. In subagent mode, EM manages order manually. In Agent
   Teams mode, task blocking enforces order automatically.

4. **Result semantics are identical.** QA verdict files have the same
   format. Sentinel findings have the same structure. EM's merge decision
   logic is mode-independent.

5. **Feature flag is the only switch.** No other configuration changes are
   needed to switch modes. `RDF_AGENT_TEAMS=true|false` is sufficient.

## 4. Agent Teams Benefits (When to Switch)

### Benefits
- **Parallel verification:** QA + Sentinel + UX Review run as independent
  teammates that self-claim work. No EM dispatch overhead.
- **Direct communication:** QA can message Sentinel directly to ask about
  a finding, without going through EM.
- **Reduced EM context:** EM's context window holds only coordination state,
  not the full result of each agent's work.
- **Natural look-ahead:** Teammates auto-claim next unblocked task, enabling
  pipeline overlap without explicit worktree management.

### Costs
- **Token cost:** ~3-4x single session per teammate. A full tier 2+ pipeline
  with 7 teammates costs significantly more than subagent mode.
- **Coordination overhead:** Task status can lag. Teammates may forget to
  mark tasks complete.
- **Experimental API:** Subject to breaking changes. No session resumption
  for in-process teammates.

### Recommendation
- Use subagent mode (default) for all production work
- Use Agent Teams mode for:
  - Large refactors with many parallel tracks
  - Debugging sessions with competing hypotheses
  - When the API stabilizes and token costs are acceptable
- Always test mode switches against a known-good pipeline run

## 5. Future: Hybrid Mode

A future enhancement could use Agent Teams for the parallel verification
group (QA + Sentinel + UX Review) while keeping subagent mode for the
sequential stages (Scope, SE, Challenger). This would capture the primary
benefit (parallel verification) without the full token cost.

Implementation: `rdf_dispatch_pipeline()` would emit subagent instructions
for sequential stages and Agent Teams instructions for parallel groups.
This requires the dispatch library to support mixed-mode output (deferred
to a future phase after the API stabilizes).
```

- [ ] **Step 5.9.2: Commit coordination spec**

```bash
git add docs/specs/task-based-coordination.md
git commit -m "$(cat <<'EOF'
Add task-based coordination specification

[New] docs/specs/task-based-coordination.md — maps file-based to task-based
  coordination for Agent Teams mode
[New] Dual-mode invariants, dependency graph, mapping table
[New] Benefits/costs analysis, hybrid mode future design
EOF
)"
```

---

## Chunk 7: Verification + Changelog

### Task 5.10: End-to-End Verification

- [ ] **Step 5.10.1: Verify all files pass lint**

```bash
bash -n lib/dispatch.sh
bash -n lib/cmd/dispatch.sh
bash -n bin/rdf
shellcheck lib/dispatch.sh
shellcheck lib/cmd/dispatch.sh
shellcheck bin/rdf
```

- [ ] **Step 5.10.2: Verify JSON files**

```bash
jq . lib/dispatch-agents.json >/dev/null && echo "agents: valid"
jq . lib/dispatch-tasks.json >/dev/null && echo "tasks: valid"
jq . adapters/claude-code/teams-meta.json >/dev/null && echo "teams-meta: valid"
```

- [ ] **Step 5.10.3: Verify subagent mode (default)**

```bash
# Status
bin/rdf dispatch status
# Expect: Dispatch Mode: subagent, 11 agents, 12 templates, 3 pipelines

# Single agent dispatch
bin/rdf dispatch agent sys-eng se-implement 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | head -5
# Expect: "### Dispatch: Senior Engineer (sys-eng)" with Mode: Subagent

# Tier 0-1 pipeline
bin/rdf dispatch pipeline 0 1 /tmp/test-project /tmp/test-plan.md 2>/dev/null | grep -c "### Dispatch:"
# Expect: 3 (scope, SE, QA-lite)

# Tier 2+ pipeline
bin/rdf dispatch pipeline 2 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | grep -c "### Dispatch:"
# Expect: 7 (scope, SE-plan, challenger, SE-impl, QA, sentinel, ux-review, UAT)

# Tier 2+ with flags
bin/rdf dispatch pipeline 2 3 /tmp/test-project /tmp/test-plan.md --no-challenger --no-ux 2>/dev/null | grep -c "### Dispatch:"
# Expect: 5 (scope, SE, QA, sentinel, UAT — no challenger, no UX)
```

- [ ] **Step 5.10.4: Verify Agent Teams mode**

```bash
# Status
RDF_AGENT_TEAMS=true bin/rdf dispatch status
# Expect: Dispatch Mode: agent-teams

# Single agent dispatch
RDF_AGENT_TEAMS=true bin/rdf dispatch agent sys-eng se-implement 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | head -5
# Expect: "### Dispatch: Senior Engineer (sys-eng)" with Mode: Agent Teams

# Tier 2+ pipeline
RDF_AGENT_TEAMS=true bin/rdf dispatch pipeline 2 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | grep "Team Setup"
# Expect: "### Team Setup" present

RDF_AGENT_TEAMS=true bin/rdf dispatch pipeline 2 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | grep "Team Teardown"
# Expect: "### Team Teardown" present

RDF_AGENT_TEAMS=true bin/rdf dispatch pipeline 2 3 /tmp/test-project /tmp/test-plan.md 2>/dev/null | grep "TaskCreate"
# Expect: multiple TaskCreate blocks
```

- [ ] **Step 5.10.5: Verify backward compatibility**

```bash
# Default mode produces output compatible with current mgr.md dispatch
bin/rdf dispatch agent sys-eng se-implement 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md 2>/dev/null | grep "subagent_type"
# Expect: rfxn-sys-eng (matching current hardcoded value)

bin/rdf dispatch agent sys-qa qa-gate 3 /root/admin/work/proj/apf /root/admin/work/proj/apf/PLAN.md 2>/dev/null | grep "model"
# Expect: sonnet (matching current hardcoded value)
```

- [ ] **Step 5.10.6: Verify no hardcoded paths in dispatch.sh**

```bash
grep -n '/root/' lib/dispatch.sh
# Expect: no matches (all paths come from templates or parameters)

grep -n '/usr/bin/' lib/dispatch.sh
# Expect: no matches
```

---

### Task 5.11: Update Changelogs

- [ ] **Step 5.11.1: Update CHANGELOG**

Add Phase 5 entries to CHANGELOG:

```
## Phase 5: Agent Teams Architecture

[New] lib/dispatch.sh — mode-aware agent dispatch abstraction (subagent vs Agent Teams)
[New] lib/dispatch-agents.json — centralized agent registry (11 agents)
[New] lib/dispatch-tasks.json — pipeline task templates with dependency chains
[New] lib/cmd/dispatch.sh — rdf dispatch subcommand (agent/pipeline/status)
[New] adapters/claude-code/teams-meta.json — CC Agent Teams configuration
[New] docs/specs/agent-teams-research.md — Agent Teams API research
[New] docs/specs/dispatch-abstraction.md — dispatch interface specification
[New] docs/specs/task-based-coordination.md — file-based to task-based mapping
[Change] lib/rdf_common.sh — add rdf_feature_enabled() feature flag helper
[Change] bin/rdf — register dispatch subcommand
[Change] canonical/agents/mgr.md — dispatch modes awareness
[Change] canonical/commands/mgr.md — dispatch abstraction reference
[New] RDF_AGENT_TEAMS feature flag (default: false, opt-in for Agent Teams)
```

- [ ] **Step 5.11.2: Update CHANGELOG.RELEASE**

Add the same entries to CHANGELOG.RELEASE.

- [ ] **Step 5.11.3: Commit changelogs**

```bash
git add CHANGELOG CHANGELOG.RELEASE
git commit -m "$(cat <<'EOF'
Update changelogs for Phase 5: Agent Teams Architecture

[Change] CHANGELOG — Phase 5 entries (dispatch abstraction, agent registry,
  task templates, Agent Teams research and specs)
[Change] CHANGELOG.RELEASE — Phase 5 entries
EOF
)"
```

---

## Summary

### Commit Sequence (7 commits)

| # | Commit | Files |
|---|--------|-------|
| 1 | Agent Teams API research document | docs/specs/agent-teams-research.md |
| 2 | Dispatch abstraction specification | docs/specs/dispatch-abstraction.md |
| 3 | Agent registry + task templates | lib/dispatch-agents.json, lib/dispatch-tasks.json |
| 4 | Dispatch library + CLI | lib/dispatch.sh, lib/cmd/dispatch.sh, lib/rdf_common.sh, bin/rdf |
| 5 | Adapter Teams metadata | adapters/claude-code/teams-meta.json |
| 6 | mgr agent + command updates | canonical/agents/mgr.md, canonical/commands/mgr.md |
| 7 | Changelogs | CHANGELOG, CHANGELOG.RELEASE |

### Acceptance Criteria

- [ ] `rdf dispatch status` reports correct mode for both flag values
- [ ] `rdf dispatch pipeline` produces valid subagent instructions (default)
- [ ] `RDF_AGENT_TEAMS=true rdf dispatch pipeline` produces valid Agent Teams instructions
- [ ] Pipeline works identically with Agent Teams disabled (zero regression)
- [ ] Dispatch abstraction works for both modes with same interface
- [ ] All agent properties match current hardcoded values in mgr.md
- [ ] All prompt templates match current hardcoded prompts in mgr.md
- [ ] Documentation maps file-based to task-based coordination
- [ ] All shell files pass `bash -n` and `shellcheck`
- [ ] All JSON files pass `jq .` validation
- [ ] No hardcoded paths in dispatch library

### Open Questions (for future phases)

1. **Hybrid mode:** Should the dispatch library support mixed subagent + Agent
   Teams within a single pipeline? (Deferred until API stabilizes)
2. **Hook scripts:** The TaskCompleted and TeammateIdle hooks referenced in
   teams-meta.json need implementation. Create as stub scripts now or defer?
   (Recommendation: defer — hooks are only relevant when Agent Teams mode is
   actively used)
3. **Token cost tracking:** Should pipeline-metrics.jsonl capture dispatch
   mode and token costs per agent? (Recommendation: yes, add `dispatch_mode`
   and `token_cost` fields when Agent Teams mode is used)
4. **Session resumption:** Agent Teams currently cannot resume. If a pipeline
   is interrupted in Agent Teams mode, should the dispatch library detect
   stale team state and offer recovery? (Deferred — experimental limitation)
